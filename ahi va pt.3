from tkinter import *
import tkinter as tk

from urllib.request import urlopen
from PIL import Image, ImageTk 
from io import BytesIO  


def portada():
    urlimagen = "https://github.com/moremedina/imagen/blob/1a541e6eba4f255ee07cf1836c938b65bdf568e7/1.png?raw=true"
    datosimagen = urlopen(urlimagen)
    imagenbinaria = datosimagen.read()
    imagen = Image.open(BytesIO(imagenbinaria))
    return imagen

def fondo():
    urlimagen = "https://github.com/moremedina/imagen2/blob/a03218e48f38f377a837a839cacc3175f003c99d/GRAFICADORA.png?raw=true"
    datosimagen = urlopen(urlimagen)
    imagenbinaria = datosimagen.read()
    imagen = Image.open(BytesIO(imagenbinaria))
    return imagen

def limpiar_ventana():
          for widget in ventana.winfo_children():
               widget.destroy()
      
def inicio():
      Label(ventana, text="Bienvenido, ¿Listo para comenzar?", font=("Segoe UI Black",20), bg="#b39ddb").place(x=450,y=180)
      Button(ventana, text="Iniciar", font=("Segoe UI",16), bg="#9575cd", command=menu, ).place(x=620,y=580)

def graficar():
     limpiar_ventana()
     etiqueta = Label(ventana, image=ventana.fondo)
     etiqueta.place(width=ancho, height=alto)

     Label(ventana, text="Ingrese el grado de la función", font=("Segoe UI Black", 20), bg="#b39ddb").place(x=580, y=300)
     grado= Entry(ventana).place(x=640,y=390)
     grado=int(grado)

     Button(ventana, text="Enviar", font=("Segoe UI Black", 20), bg="#9575cd").place(x=648,y=450)
     
     for grado 

     Label(ventana, text="Ingrese el coeficiente", font=("Segoe Black UI", 20), bg="#b39ddb").place(x=300, y=400)
     coeficiente= Entry(ventana)
     coeficiente=int(coeficiente)
     

     Button(ventana, text="Enviar", font=("Segoe UI Black", 20), bg="#9575cd").place(x=648,y=450)

def puntos():
     limpiar_ventana()
     etiqueta = Label(ventana, image=ventana.fondo)
     etiqueta.place(width=ancho, height=alto)

     Label(ventana, text="Ingrese los puntos específicos de la función", font=("Segoe UI Black", 20), bg="#b39ddb").place(x=400,y=300)
     Entry(ventana)
     respuesta=Entry(ventana).place(x=620,y=390)
     Button(ventana, text="Enviar", font=("Segoe UI Black", 20), bg="#9575cd").place(x=628,y=450)

def raíces():
     limpiar_ventana()
     etiqueta = Label(ventana, image=ventana.fondo)
     etiqueta.place(width=ancho, height=alto)

     Label(ventana, text="Ingrese la función", font=("Segoe UI Black", 20), bg="#b39ddb").place(x=580,y=300)
     Entry(ventana)
     respuesta=Entry(ventana).place(x=640,y=390)
     Button(ventana, text="Enviar", font=("Segoe UI Black", 20), bg="#9575cd").place(x=648,y=450)

def suma():
     limpiar_ventana()
     etiqueta = Label(ventana, image=ventana.fondo)
     etiqueta.place(width=ancho, height=alto)

     Label(ventana, text="Ingrese las funciones", font=("Segoe UI Black", 20), bg="#b39ddb").place(x=575, y=300)
     Entry(ventana)
     respuesta=Entry(ventana).place(x=650,y=390)
     respuesta2=Entry(ventana).place(x=650,y=430)
     Button(ventana, text="Enviar", font=("Segoe UI Black", 20), bg="#9575cd").place(x=658,y=495)


def menu():
     limpiar_ventana()
     etiqueta = Label(ventana, image=ventana.fondo)
     etiqueta.place(width=ancho, height=alto)

     Label(ventana, text="Menú principal", font=("Segoe UI Black", 20), bg="#b39ddb").place(x=600,y=100)
     Button(ventana, text="Graficar función", font=("Segoe UI",16), bg="#9575cd", command=graficar, ).place(x=620,y=250)
     Button(ventana, text="Graficar puntos específicos sobre la función", font=("Segoe UI",16), bg="#9575cd", command=puntos).place(x=498,y=350)
     Button(ventana, text="Encontrar raíces aproximadas de la función", font=("Segoe UI",16), bg="#9575cd", command=raíces).place(x=501,y=450)
     Button(ventana, text="Sumar", font=("Segoe UI",16), bg="#9575cd", command=suma).place(x=670,y=550)
    


ventana=tk.Tk()
ventana.title("Menú")
ventana.configure(bg="#b39ddb")
ancho = ventana.winfo_screenwidth()
alto = ventana.winfo_screenheight()
ventana.geometry(f"{ancho}x{alto}")

imagen = portada()
imagenRedimensionada = imagen.resize((ancho, alto), Image.Resampling.LANCZOS)
portadaTk = ImageTk.PhotoImage(imagenRedimensionada)
ventana.portada=portadaTk

imagen = fondo()
imagenRedimensionada = imagen.resize((ancho, alto), Image.Resampling.LANCZOS)
fondoTk = ImageTk.PhotoImage(imagenRedimensionada)
ventana.fondo=fondoTk

etiqueta = Label(ventana, image=ventana.portada)
etiqueta.place(width=ancho, height=alto)

inicio()



ventana.mainloop()
